<%= simple_form_for([@task, sellers_submission],  :method=>'put') do |f| %>
	<%= f.input :feedback, label: false, placeholder: "Add your feedback" %>
	<%= f.button :submit, "Approve Submission", class: "green" %>
	<%= f.button :submit, "Needs Revision", class: "orange" %>
<% end %>